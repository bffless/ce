---
name: real-skill
description: Fixture skill used by the broken rule-set fixture's skills cross-ref test.
---

# Real Skill

Placeholder skill body — exists only so `validateRuleSet`'s skills cross-ref has a real
skills root with one legitimate entry, alongside the deliberately-missing
`does-not-exist` reference in `rules/api/e/post.rule.yaml`.
